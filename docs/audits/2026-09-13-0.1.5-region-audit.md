# 0.1.5 分区审计（协同工作文档）

> 本文档是**进行中**的审计记录：每测一项就写一条，带命令与原始证据；不做"看起来没毛病"的结论。
> 审计者看不到截图，**一切结论必须有页面内可程序化测量的证据**（几何 / elementFromPoint 命中 / 计算样式 / CSSOM 规则裁决 / 用户事件派发后的状态变化）。
> 环境：Termux，无 root；宿主 `@deepseek-ai/dsh` **0.1.5-rc.1**；插件工作树含未提交改动（另一会话所改）。

## 0. 方法与复现基线

> **审计纪律（2026-09-13 立）**：每条结论必须能回答"我用什么命令量的、量到多少、反例是什么"。探针里的固定 `sleep` 等就绪一律改为**轮询就绪条件**（`waitReady`：`readyState==='complete'` ∧ frame 标记 ∧ 插件样式恰好 1 个 ∧ 可选 active 阶段）——本轮曾因"等 9 秒"误判出「最小环境页面不起壳」的假结论。

- 最小复现 profile（用户口径：**仅加载两个插件**）：`~/.dsh/profiles/mob-audit` = `@deepseek-ai/dsh-base` + `@deepseek-ai/dsh-web-app` + `dsh-web-mobile`（link: 工作树）。第三方噪音（dsh-web-all 等）全部排除。
- 启动：`dsh --profile mob-audit --port 3082`（日志里打印 `http://127.0.0.1:3082/?token=…`，token 同时用于浏览器认证）。
- 浏览器：headless chromium + CDP（`/data/data/com.termux/files/usr/lib/chromium/chrome`），`Emulation.setDeviceMetricsOverride{390x844, mobile:true}` + `setTouchEmulationEnabled{maxTouchPoints:5}`（**必须开触摸仿真**，否则 `(pointer: coarse)` 不成立）。
- 探针脚本（临时，不入库）：`~/tmp/diag/{matrix,audit1,drawer-dom,regions2,cssdiag,matched}.mjs`。
- 判定口径：元素存在 ≠ 生效。每项需同时满足「在 DOM」「几何>0 且非隐藏」「中心点 elementFromPoint 命中自身或其子树」「计算样式与规格一致」。

## 1. 宿主结构基准（0.1.5-rc.1，active 会话，390×844）

实测骨架（命令：`~/tmp/diag/skeleton.mjs`）：

```text
body > #root > [data-slot=root] > div.pI_x6G_frame[data-dsh-frame][data-sidebar-collapsed=true][data-mobile-nav="frame"]
  ├─ div.pI_x6G_sidebarCol[data-pane="sidebar"]           ← 抽屉本体（插件只加标记，未改结构）
  │    └─ [data-slot=sidebar] > div.hHd-Xa_root(.hHd-Xa_collapsed|.hHd-Xa_quietBars)
  │         ├─ .hHd-Xa_logoRow > button.hHd-Xa_iconButton.hHd-Xa_toggle[aria-label="Open sidebar"]
  │         │                    [data-dsh-responsive-part="sidebar-toggle"]   ← 宿主 rail 折叠开关
  │         ├─ button.hHd-Xa_newSession
  │         ├─ .hHd-Xa_regionArea > [data-slot=sidebar.workspaces] > .bhn1Oq_root
  │         │     ├─ .bhn1Oq_sectionHeader（含 .bhn1Oq_search / .bhn1Oq_headerActions）
  │         │     └─ .bhn1Oq_listArea > .bhn1Oq_list[role=tree] ← 会话行 .YDXeBa_sessionRow[aria-selected]
  │         └─ .hHd-Xa_footArea > .hHd-Xa_footerActions > [data-slot=sidebar.footer.action]
  │                             + .hHd-Xa_settingsArea > [data-slot=sidebar.settings]
  ├─ div.pI_x6G_centerCol[data-pane="conversation"] > [data-slot=main] > … div.wSkVaW_root[data-phase=active]
  │    ├─ header.wSkVaW_header[data-dsh-responsive-part="conversation-header"]  ← 97px，两行网格
  │    │    （宿主 0.1.5 自带：grid-template-columns: minmax(0,1fr) auto；rows: minmax(32px,auto) minmax(44px,auto)；padding: 8px 8px 0 60px !important）
  │    └─ .wSkVaW_body > .wSkVaW_scrollBody
  └─ div.pI_x6G_overlayLayer[data-shell-overlay] > [data-slot=shell.overlay]
```

关键代际差异（相对 0.1.1-rc.2）：**宿主把窄屏侧栏重做成原生 overlay 抽屉**（`.pI_x6G_sidebarCol` 计算 `position:absolute; z-index:1100`；collapsed=52px rail 且 `pointer-events:none`；expanded=280~321px 且 `pe:auto`；frame 单轨 grid），并且**自带一套窄屏响应层**（`@media (max-width:768px)`，大量 `!important`），其中：

- `[data-dsh-frame][data-sidebar-collapsed] [data-pane="sidebar"] [data-dsh-responsive-part="sidebar-toggle"] { display: inline-flex !important }`（保证 rail 上的开关可见）
- `[data-dsh-frame][data-sidebar-collapsed] [data-pane="sidebar"] { width:52px !important; pointer-events:none; transform:none; background:transparent !important }`
- `[data-dsh-frame] { grid-template-columns: minmax(0,1fr) !important }`
- `document` 捕获层点击关闭：`installMobileSidebarDismiss()` —— 侧栏展开时点击**侧栏外**任意位置 → `preventDefault + stopPropagation + toggle.click()`（≤768px 生效）

## 2. 分区审计矩阵（进行中）

图例：✅ 通过 / ❌ 失败 / ⚠️ 通过但语义存疑 / ⏳ 未测

### 2.0 环境隔离矩阵（**关键：三套环境行为不同，混用会得出相反结论**）

| 环境 | 组合 | `data-dsh-frame` / `data-pane` | 抽屉列宽来源 | 宿主 rail 开关 | 实测抽屉 |
|---|---|---|---|---|---|
| A 最小 profile `mob-audit`（用户口径「仅两个插件」） | `@deepseek-ai/dsh-base` + `dsh-web-app` + `dsh-web-mobile` | **都不存在**（宿主 0.1.5 安装包里 0 命中） | 宿主 root inline `width:280px` | **不存在**（`data-dsh-responsive-part` 缺席） | 280px 列 / 280px 内容，**无空白** |
| B 全量 profile（用户实际在跑：3080/3099） | web profile 20+ 插件，含 `@linxin666/dsh-web-all` | 存在（`data-pane` ×2） | **第三方响应层 `width:320px !important`** | 存在 | **320px 列 / 280px 内容 → 40px 空白带** |
| C 之前审计用的 web profile 对照 | 同 B | 同 B | 同 B | 存在，且压在插件按钮上 | 同 B |

**结论：`data-dsh-frame` / `data-pane` / `data-dsh-responsive-part` 不是宿主 0.1.5 的契约，而是 `@linxin666/dsh-web-all` 响应层的注入**（取证：`python3` 全树扫描宿主安装包，`data-dsh-frame`/`data-pane` 各 0 命中；`dsh-client-ui-layout/lib/client.js` 只定义 `pI_x6G_frame|sidebarCol|centerCol|rightbarCol|overlayLayer`）。**只装两个插件时依赖这些属性的插件规则全部空转**——这是"很多功能用不了"的第一类根因。

### 2.1 外壳与抽屉（§frame/drawer）

| 项 | 结果 | 证据 |
|---|---|---|
| 样式标签恰好 1 个 | ✅ | `style[data-plugin="dsh-web-mobile"]` ×1 |
| frame 标记落在宿主 frame | ✅ | `[data-mobile-nav="frame"][data-dsh-frame]` ×1 |
| grid 三轨 | ✅ | computed `grid-template-columns: 390px 0px 0px` |
| 收起态 rail 52px / pe:none | ✅ | rect `[0,0,52,844]`，`pointer-events:none` |
| 边缘滑开抽屉 | ✅ | collapsed 标记移除；drawer rect `[0,0,320,844]`，`pe:auto`，z=1300 |
| 抽屉内命中 | ✅ | `elementFromPoint(80,400)` 落在抽屉内（命中 `.YDXeBa_title`） |
| 抽屉内会话行 | ✅ | `.YDXeBa_sessionRow` ×5，`aria-selected=false/true` 正常（**上次判 FAIL 是我选择器错用 `[role=treeitem]`，非缺陷**） |
| 打开时 backdrop 存在且铺满 | ✅ | `[data-mobile-nav="backdrop"]` 390×844 |
| 点抽屉外关闭 | ✅ | 宿主捕获层 `installMobileSidebarDismiss` + 插件 backdrop 双路径 |
| 收起态无 backdrop | ✅ | count=0 |

### 2.2 会话 header 区（绘制名称/工具行）与抽屉开关

| 项 | 结果 | 证据 |
|---|---|---|
| 插件 toggle 按钮存在 | ✅ | `[data-mobile-nav="toggle"]` rect `[8,12,28,28]` |
| **插件 toggle 可点（命中自身）** | ❌ | 中心点命中 `button.hHd-Xa_iconButton.hHd-Xa_toggle`（宿主 rail 开关，44×44 位于 `[10,14]`）→ 用户点它实际点到宿主开关 |
| 插件 Files 按钮可点 | ✅ | rect `[256,16,28,28]`，hitSelf=true |
| 宿主 rail 开关是否被隐藏 | ❌ | 插件规则 `[class*="hHd-Xa_toggle"]{display:none !important}` **未生效**：计算 `display:flex` |
| 抽屉展开时插件 toggle 可点 | ❌ | 命中 `.hHd-Xa_brandIdentity`（宿主 brand 按钮覆盖同一位置） |
| 宿主 header 布局（0.1.5 自带） | ⚠️ | grid 两行、`padding-left:60px`（给 rail 开关留位）；插件 header 规则与其叠加 |
| 顶部 hero 阶段 FAB | ⏳ | active 阶段按设计不建（hero 才有）；hero 阶段需单独验 |

**失败根因（已用 CDP matched-styles 裁决，见 §3-RC1）**：属性平局后按文档顺序，宿主更晚注入 → 宿主赢。

### 2.3 输入区（composer）

| 项 | 结果 | 证据 |
|---|---|---|
| composerStack 可见 | ✅ | `[0,672,388,172]` |
| 卡片在视口内 | ✅ | card `[16,716,356,98]`（底 814 < 844） |
| 文档不滚动（safe-area 配对） | ✅ | `scrollHeight-clientHeight=0` |
| 输入域字号（iOS 下限，非 iOS 时 14px 正常） | ✅/⏳ | `data-composer-input` computed 14px（无 iOS 标记时正确）；iOS 场景待单独验 |
| 底部行三件套几何 | ⏳ | 需按规格逐项断言（`_add` 28 / ContextMeter 28 / `_primary` 34） |

### 2.4 状态行 / TPS

| 项 | 结果 | 证据 |
|---|---|---|
| `[data-mobile-nav="stats"]` 标记注入 | ❌ | count=0（0.1.5 composer stack 内没有匹配 `/(turns|steps|LLM|轮|步)/` 的候选行） |

### 2.5 设置 / 市场 / 文件 / 预览 / 子代理 / 删除会话

| 项 | 结果 | 证据 |
|---|---|---|
| 抽屉 footer 动作按钮（Files / Session log） | ✅ | `[data-slot=sidebar.footer.action]` 内 2 个按钮，各 124×34 |
| 设置入口按钮 | ✅ | `.VOzbGW_trigger` 260×42，`aria-haspopup=dialog` |
| 设置弹层布局（行卡片化/导航） | ⏳ | 待按 compat 规格逐项断言 |
| 市场（dshmarket）规则 | ⏳ | 最小 profile 无市场插件，需在含市场 profile 单独验 |
| 文件浏览器 / 预览全屏 | ⏳ | 依赖 dsh-web-ui-all，最小 profile 缺席 |
| 子代理芯片触摸 | ⏳ | 同上（宿主自带 subagent，需在含宿主 UI 的会话中验） |
| 删除会话菜单项 | ⏳ | 需展开抽屉 + 打开行 ⋯ 菜单 |

### 2.6 抽屉宽度（用户报「抽屉空白区域宽度与 UI 宽度不一致」）

**现象（实测，非目测）**：全量 profile 上抽屉列 **320px**、宿主侧栏内容 **280px** → 抽屉内部右侧 **40px 空白带**，且该带内 `elementFromPoint` 命中的是空壳列 `pI_x6G_sidebarCol`（**点它既不关抽屉也无任何反应**）。

**A/B 对照（活页面注入，不改源码；`~/tmp/diag/ab-width.mjs`）**：

| 注入 | 列宽 | 内容 | 内部空白 |
|---|---|---|---|
| baseline（无注入） | 320px | 280px | **40px** |
| `[data-dsh-frame] [data-pane="sidebar"]{width:max-content!important}` | 280px | 280px | **0** |
| 同上 `fit-content` / `280px` | 280px | 280px | 0 |
| 只改插件自身 `inset`（去掉左右边沿钉定） | 280px | 280px | 0（但破坏抽屉贴左，不采用） |

**断言门（`~/tmp/diag/assert-width.mjs`，6 断言）**：未修 → `no-internal-blank`、`beyond-drawer-is-backdrop` 2 项 FAIL；注入 `width:max-content !important` → 仅剩 `beyond-drawer-is-backdrop` FAIL，**且该项在修复后语义反转**：修复前命中空壳列（死区），修复后命中 `null`（CSS 像素，因为列已缩到 280px，x=292 处无元素可命中），需改写断言为"点该处是否关抽屉"而不是"命中谁"。

**判定**：宽度**不是插件定的**——插件规则 `width:max-content`（无 `!important`）+ `max-width:92vw` 是**上限**且从未命中；宿主 root 的 inline `width:280px`（`computeColumns`: 收起 56px / 展开 clamp 264–420，默认偏好 280）才是内容宽；把列撑到 320px 的是**第三方响应层**。修法=给插件那条 `width/max-width` 补 `!important`（同特异度下 `!important` 胜非 `!important`），使抽屉**永远贴合内容**，从而在两种 profile 上都不留空白带。

## 3. 根因清单（每条必须有取证）

### RC1 · 插件规则与宿主 `!important` 平局后由**文档顺序**决定，宿主更晚注入 → 宿主赢

- 现象：插件 `[class*="hHd-Xa_toggle"]{display:none !important}` 在 CSSOM 里存在、media 条件匹配、元素 `matches()` 为 true，但计算样式仍 `display:flex`。
- 取证（`~/tmp/diag/matched.mjs`，CDP `CSS.getMatchedStylesForNode`）：同一元素上同时匹配
  - 宿主：`[data-dsh-frame][data-sidebar-collapsed] [data-pane="sidebar"] [data-dsh-responsive-part="sidebar-toggle"] { display:inline-flex !important }`（特异度 0,4,0，媒体 ≤768px）
  - 插件：`[class*="hHd-Xa_toggle"] { display:none !important }`（特异度 0,2,0）
- 裁决：**平局不看特异度，看注入顺序**；宿主样式表在插件 `<style>` 之前，但 `!important` 同源同权重时比较声明顺序——实测宿主后来居上（宿主框架样式与插件注入顺序在不同宿主版本可能变化，属脆弱依赖）。
- 影响：任何"插件 `!important` vs 宿主 `!important`"的对抗都不稳定，必须**显式提高特异度**（例如加 `:root` / `html` 前缀 / 组合到宿主结构上）。

### RC2 · 插件 toggle 与宿主 rail 开关**几何重叠**且层级更低

- 现象：面板收起时用户点左上角，命中宿主 `hHd-Xa_iconButton hHd-Xa_toggle`（44×44 @ `[10,14]`），不是插件 `[data-mobile-nav="toggle"]`（28×28 @ `[8,12]`，z=2）。
- 影响：点"我们的抽屉按钮"实际触发宿主开关；展开后同位置又被 `.hHd-Xa_brand`（252×24 的"新建会话"品牌按钮）覆盖 → 两种状态下都点不到。

### RC1b · 依赖第三方 shim 的宿主属性 = 只在装了该第三方时可用（**本轮最重要**）

- 插件大量布局规则/效果依赖 `data-dsh-frame`、`data-pane="sidebar|conversation|details"`、`data-dsh-responsive-part`。
- 取证：宿主 0.1.5 安装包（`/data/data/com.termux/files/usr/lib/node_modules/@deepseek-ai/dsh`）全树扫描 → `data-dsh-frame` 0 命中、`data-pane` 0 命中；这些属性由 `@linxin666/dsh-web-all` 的 `COLUMN_SHIMS` + `RESPONSIVE_CSS` 注入。
- 影响：**用户"仅加载两个插件"时，凡以这些属性为锚的规则全部静默失效**（不是报错，是"看起来没毛病但功能没了"）。
- 待办：把这类锚点改为**同时兼容两种来源**（宿主结构类名子串 + 第三方属性），或在缺失时降级到结构选择器。

### RC3 · 状态行（TPS）在 0.1.5 不再命中插件的启发式

- 插件在 `[data-phase] [class*="_root"]` 中寻找位于 `_composerStack` 内、含 `/(turns|steps|LLM|轮|步)/` 文本、无 button、无输入框的行。
- 实测 0.1.5 的 composer stack 子结构为 `lXshSW_root`（38px 提示条）、`uV2eYG_root > uV2eYG_card`（输入卡）、`bOPqQW_root`（26px 行）——状态行候选文本不含上述关键词 → 标记未注入（count=0）。

## 4. 待办（按区域推进，测一项记一项）

- [ ] hero 阶段（无会话）全套：FAB / 空态 composer / 设置可用性
- [ ] 设置弹层分区：nav 竖排、行卡片化、市场头部、叉号可达、底部 sheet 几何
- [ ] composer 底部行三件套 + 模型条 + 子代理形态
- [ ] header 工具行：视图 tab 横滚、拥挤保护、计数芯片
- [ ] 会话行 ⋯ 菜单 + 删除会话（注入/弹窗/端点）
- [ ] 手势回归门 16 场景 + 主探针 32 断言（最小 profile 上重跑）
- [ ] 与 HEAD（v2.4.1 构建）A/B，输出「回归清单」
- [ ] 决策记录：0.1.5 上"夺回抽屉" vs "让位宿主"的取舍（含用户已知偏好：要自己的抽屉 + 手势）


---

## 5. 本轮修复（已落源码 + 已验证）

**修复**：`src/client/styles/layout.css.ts` 抽屉列规则

```diff
-  [data-mobile-nav="frame"] > :first-child {   width: max-content;    max-width: 92vw;    …
+  :root [data-mobile-nav="frame"] > :first-child { width: max-content !important; max-width: 92vw !important; …
```

两处改动、都必要：

1. `!important`：对手 `[data-dsh-frame] [data-pane="sidebar"] { width: min(88vw,320px) !important }`（**来自 `@linxin666/dsh-web-all`**）带 `!important`，无 `!important` 的声明必然输。
2. `:root` 前缀：与我抢同一条声明的是**同重要级**（双方 `!important`）的第三方规则，胜负由"文档顺序 + 特异度"决定。提高一个特异度后不再依赖"我们的样式标签恰好更晚注入"这一脆弱前提（仓库既有风格一致）。

**因果反证（同一探针对比，非关联）**：

| 构建 | col | inner | blank | 内容行可点 |
|---|---|---|---|---|
| HEAD 版（`width: max-content`） | 320 | 280 | 40 | ✗ |
| 修复版（`+ !important`） | 280 | 280 | 0 | ✓ |

**回归结果（全量 profile 实例 3083，实装 bundle rev `af63f947533a`）**：

- `pnpm verify` ✅ ／ `pnpm test:core` ✅ **83 pass / 0 fail** ／ `pnpm build` ✅（lib 已刷新）
- `pnpm smoke:cdp`（主探针）：**pass=13 skip=1 fail=0 new=0**，`page.errors count=0`；其中 `mobile.drawer.backdrop drawerWidth=280`
- `node scripts/cdp-swipe-failures.mjs`（手势门）：A1–B3、C1、E1/E1c/E2、**E3c**、F1/F2 **全 PASS**
- 自建场景门（`~/tmp/diag/scenarios.mjs` / `desktop-zero.mjs`）：手机 390 触摸 4/4 PASS（280/280、tap-right-closes @x=294）、平板 900 触摸 2/2 PASS、**桌面 1280 鼠标零影响 7/7 PASS**（frame 无插件标记、注入控件 `display:none`、列 `position:static/z auto/transform none`、无文档滚动）

## 6. 附带发现与未决项

### 6.1 手势门 E3 在 0.1.5 变成"观察项"（**不是回归**，已因果判定）

- 现象：向 Lexical 编辑面（`[data-composer-input]`，`contenteditable=true`）注入 `Range` 选中前 20 字符后，*什么都不做* 也会在 ~1.2s 内变成 `alive=false`（`text` 长度同时归 0）——**Lexical 自己的状态对账清掉了我们手动写的 DOM**，与手势无关；同理，清除选区后同一手势（E3c）正常打开抽屉 = 让位语义在 0.1.5 依旧成立。
- 插件侧判定函数 `selectionOwnsStroke()` 读 `window.getSelection()` + `activeElement` 的 `selectionStart/End`（`sidebar-swipe.ts`），对 Lexical 场景仍正确；缺的只是"探针能稳定构造出选区"这一前提。
- 处置：`scripts/cdp-swipe-failures.mjs` 的 E3 已改为**两代通吃**（`[data-composer-input]` 优先、`textarea` 回退；按 `TEXTAREA` / contenteditable 分别读取与断言，标签里带代际 `DIV/TEXTAREA`），0.1.5 上记为 OBS 并保留 E3c 作为让位语义的 PASS 证据。

### 6.2 已发现、尚未修的项（按优先级）

1. **状态行/TPS 标记在 0.1.5 不注入**（§2.4）：`stats-line` 的候选行启发式（文本需匹配 `turns|steps|LLM|轮|步` 且无 button/输入框）在 0.1.5 的 composer stack 结构上 0 命中。需要重新锚定（0.1.5 stack 子节点：`lXshSW_root` 提示条 38px / `uV2eYG_root>uV2eYG_card` 输入卡 / `bOPqQW_root` 26px）。
2. **依赖第三方 shim 的锚点**（RC1b）：`data-dsh-frame` / `data-pane` / `data-dsh-responsive-part` 全部来自 `@linxin666/dsh-web-all`；只装两个插件时以它们为锚的规则静默失效。需给出"结构类名 + 属性"双通道锚定或降级路径。
3. **宿主 rail 开关与插件 toggle 几何重叠**（RC2）：装了 dsh-web-all 时抽屉收起态点击左上角命中的是宿主 `hHd-Xa_iconButton hHd-Xa_toggle`（44×44 @10,14，z1100），不是插件按钮；只在装了该第三方时复现。
4. **抽屉展开态插件 toggle 被宿主 brand 覆盖**（`hHd-Xa_brandIdentity`）：两个 profile 都复现；用户点左上角"自己的按钮"实际点到宿主新建会话按钮。
5. 未测区（待续）：设置弹层分区、市场头部、文件/预览 sheet、子代理芯片、会话行 ⋯ 菜单删除、hero FAB、iOS 聚焦放大。

## 7. 探针工具链（本轮新增/修正，均在 `~/tmp/diag/`）

- `lib.mjs`：共享启动器。**就绪一律轮询**（`waitReady`：`readyState=complete` ∧ 插件 frame 标记 ∧ 样式标签=1 ∧ 可选 `phase=active`）；建 CDP 会话时顺序必须是 `DOM.enable` → `CSS.enable`（`CSS.enable` 先于 `DOM.enable` 会报 "DOM agent needs to be enabled first"）。
- `assert-width.mjs`：抽屉宽度门（6 断言）。
- `scenarios.mjs` / `desktop-zero.mjs`：手机/平板/桌面三场景门。
- 排障记录：**`DSH_PROBE_URL` 带 `?token=` 会让主探针永远 "page load timed out"**——服务器 303 剥掉 query 后 `location.href` 是裸地址，而探针判据是 `href.startsWith(DSH_PROBE_URL)`。正确姿势：`DSH_PROBE_URL=http://127.0.0.1:<port>/` + `DSH_PROBE_COOKIE='dsh-auth-<id>=<value>'`（cookie 名/值从 `curl -D - '<url>/?token=…'` 的 `set-cookie` 抄）。

## 8. 维护提醒（与既有文档的冲突，需收口）

- `AGENTS.md` 与 `docs/maintenance/pitfalls.md` 的「0.1.5 让位」条目描述的是**已被工作树删除**的机制（`data-mobile-nav-gen="native-drawer"`；源码 grep 0 命中），且把 `data-dsh-frame`/`data-pane` 当成宿主契约（实为第三方注入）。收口时必须改写，否则下一位维护者按图索骥必错。
- `AGENTS.md` 的探针/测试计数需同步（`tests/` 现 13 个文件；主探针 13 PASS/1 SKIP 的实际构成）。


---

## 9. 第二轮：用户真机口述症状 → 逐条钉死（2026-09-13 续）

用户原话涉及的四个症状，逐个给出**机制级结论**（不是"看着像"）：

### 9.1 「文件/仓库列表按钮点了没用」——已修（本轮第二个修复）

- **症状复现**：`[data-mobile-nav="files"]`（头部 8,12 / 256,16 两个按钮之一）点击后 **delta 全空**（无面板、无菜单、无任何状态变化）。
- **旧实现**：按钮只切 frame 上的 `data-aionui-explorer-open`，那是 **`@linxin666/dsh-web-ui-all` aionui explorer 列的 marker**；本机（以及"只装两个插件"的场景）**根本没有 explorer 列**（实测 `[data-aionui-explorer-col]` = false），于是点了等于没点。
- **官方真实形态**：0.1.5 的文件浏览在**官方右侧栏**（`rightbar` 槽 → `P3OORG_panel` → `_surface_17p4l/_pane_17p4l`，content 为 `Files/<项目路径>` 目录树；`dsh-client-ui-sidebar-files` / `directory-picker` 两个官方插件在场）。
- **为什么入口点不到**：宿主把唯一的开按钮渲染在 `wSkVaW_headerCorner` 里，而该容器 **computed `display:none`** → 按钮 `_1kL45W_button`（宿主 CSS 写死 `28x28`）实测 **0×0**；`Open right sidebar` 的 `display:inline-flex` 是假象（父级不渲染）。右缘滑动也**打不开**（宿主不支持该手势，实测 slide 后 panel 仍在 x=390）。
- **验证"功能还活着"**：程序化 `document.querySelector('[data-sidebar-right-expand]').click()` → 面板真的滑入 `[0,0,390,844]`，命中测试为 panel 内部，标题 `Files`，行内容 `.codegraph/.dsh-vision-toolkit/.git/.github/.local-tests/assets/docs/lib`。
- **修法**（复用宿主动作，不对抗布局）：新增 `src/client/components/open-files-panel.ts`，两个 Files 入口（头部 `MobileNavToggle` / 抽屉页脚 `MobileDrawerFooter`）统一走 `openFilesPanel()`：**有宿主 opener 就点它**（`data-sidebar-right-expand`，鸭子类型判 `click`），**没有才回退**到旧的 aionui explorer marker（此时才需要让出 preview 并关抽屉）。
- **验证**：单测 `tests/open-files-panel.test.ts` 4 断言（宿主优先 / 回退 marker / 双缺省 no-op / 非可点匹配不抛异常）；真机门 `~/tmp/diag/verify-files.mjs` 5/5 PASS（面板初始 x=390 → 点击后 x=0、宽 390、命中 panel、树行 ≥3）。
- 说明：`data-sidebar-right-expand` 是宿主**显式命名**的稳定钩子（源码 `ExpandButton` 里与 `aria-label` 并列写入），比哈希类名安全；升级对账点=该属性是否继续存在。

### 9.2 「右上角三个点点了没用」——**未复现，需要用户确认指哪个**

- 实测全量环境 header 里**只有一个 popup 控件**：`button.nL4_yW_moreButton[aria-label="More actions"]`（rect 330,8 44×44，`aria-haspopup="menu"`）。真实触摸点击 → `[role="menu"]` 打开（`_list_1nxmc_8`，位于 156,56 218×52，项数=本会话可用动作数）。
- 其余右上候选：官方 `Open right sidebar`（0×0 隐藏，见 §9.1）、官方 `Chat/Trajectory` 标签（可点）。
- 因此"点了没用"的两种可能：(a) 用户指的是那个被隐藏的官方右侧栏按钮（＝§9.1）；(b) 菜单在用户会话上**项多/定位异常**（本机只有 1 项，样本不足）。**按 Karpathy 准则不猜**：已在给用户的回复里请其确认是哪一个，或贴一下点开后的现象。

### 9.3 「顶部一行乱七八糟、元素尺寸自适配失效」——部分属宿主设计，需用户确认感受点

实测（390×844，全量环境，active 会话）：

| 元素 | rect | 关键计算样式 | 判定 |
|---|---|---|---|
| header | `0,0,390,97` | grid `314px 0px / 44px 44px`，padding `8px 8px 0 60px` | 宿主 0.1.5 把标题行与视图标签行拆成两行；左 60px 是宿主给已隐藏的 rail 开关预留 |
| titleRow | `60,8,314,44` | flex, gap 2px, min-width 0 | 子元素合计 322px vs 可用 314px（**8px 溢出**，由 `titleCluster flex:1 1 auto` 吸收） |
| crumbs（路径） | `80,16,86,28` | `overflow:hidden; text-overflow:ellipsis; white-space:nowrap; flex:1 1 0` | 路径被省略号截断＝设计 |
| titleCluster（按钮组） | `80,16,248,28` | flex, gap 2px | 内含 toggle/files/… |
| More actions | `330,8,44,44` | — | 贴右缘 |

- 结论：**没有发现"尺寸自适配完全失效"**（无横向溢出、无文档滚动、无 0 宽可见控件），但 header 高度 0.1.5 由 57→97px（多出标签行）与标题截断是**宿主行为**；若用户"乱"的观感指的是这两点，需要单独讨论是否由插件收紧（例如隐藏标签行/压缩 padding），而不是当作 bug 直接改。

### 9.4 本轮附带发现（结构性，影响面更大）

1. **宿主在窄屏把 `wSkVaW_headerCorner` 整块 `display:none`** —— 官方自己的右侧栏入口因此不可达；凡"官方给窄屏适配过的控件"都要先查它是否被父容器关掉。
2. **官方状态行在 0.1.5 是按钮**（`bOPqQW_pill`，带 `aria-haspopup="dialog"`，label 就是指标文本）——这与 §2.4 的 TPS 修复同源，见 §9.5。
3. `rightbar` 面板在窄屏是**全屏滑入面板**（390×844，`P3OORG_panel` z=40），且**没有自己的全屏遮罩**——它与我们插件的左侧抽屉 backdrop 是两套语义，叠加时需单独验证。
4. 侧栏底部无目录/文件树（`directoryFlow` 空、`_treeRow` 0 条）：官方"文件列表"**只在右侧栏**，不在左侧抽屉。

### 9.5 TPS/状态行修复（本轮第一个修复，与 §2.4 呼应）

- 旧逻辑两处失效：① `statsAnchorAlive` 要求 marker 是 `_composerStack` 的**直接子级**；② 搜寻同样要求直接子级，而 0.1.5 把状态行放在 `uV2eYG_root > bOPqQW_root`（嵌套更深）。③ 还有一条"候选不得含任何 button"的护栏，而 0.1.5 **把这行本身做成了两个 popup 按钮** → 唯一应该被标记的行被自己排除（探针输出 `rejected: ["hasButton"]`）。
- 修法：快路径改回**纯元素自身判定**（`el.closest('[class*="_composerStack"]') !== null`，保持 Node 单测可跑），搜寻范围放宽为 stack 的**后代**，并把"含按钮即排除"改为"**按钮全部为 popup 菜单/弹窗时才允许**"。
- 验证：`[data-mobile-nav="stats"]` 现落位于 `bOPqQW_root`（16,812 356×28），插件 CSS 生效（`overflow-x:auto`、`white-space:nowrap`、scrollW 396 > clientW 356），主探针 13 PASS / new=0；`tests/stats-line-fastpath.test.ts` 保持通过（曾因我第一版用全局 `document` 而当场翻红，已修正——该测试就是这条契约的守卫）。
- 备注：0.1.5 的 TPS 读数文案已变（`103 tok/s`，不再有 `TPS 12.3` 前缀），`moveTps` 的 `/^TPS\s+\d/` 因此不再命中；当前形态下**无独立 TPS 节点**，故不影响显示，但宿主若恢复 `TPS` 文案则应重新对账该正则。

### 9.6 本轮回归汇总

| 门 | 结果 |
|---|---|
| `pnpm verify` | ✅ |
| `pnpm test:core` | ✅ **87 pass / 0 fail**（新增 `tests/open-files-panel.test.ts` 4 断言） |
| `pnpm build` | ✅（26 modules，rev `06f05156f4fb`） |
| `pnpm smoke:cdp` | ✅ 13 pass / 1 skip / **new=0**，page.errors=0 |
| 真机门 `verify-files.mjs` | ✅ 5/5 |
| 真机门 `verify-stats.mjs` | ✅ marker=1 落位正确 |


---

## 10. 第三轮：顶栏（用户截图 + 逐条实测）

用户给了一张顶栏截图（含 OCR 描述）。据此逐项复现，得两个真实缺陷与一个方法学教训。

### 10.1 模式标签被压成文字碎片（已修）

- **用户所见**：顶栏中部出现「点阵图案 + 一个突兀的字 + 个后台任务运行中 + ⌄」。前端无法看图，改由文案反查定位：`{count} 个后台任务运行中` 来自官方 `@deepseek-ai/dsh-client-ui-jobs`（`count.live.one/other`），其触发器为一个按钮 `QsffPG_trigger`，内部 = `QsffPG_triggerDot` 小圆点 + `QsffPG_count` 文案 + 展开箭头 → **用户描述的"点阵 + ⌄"就是这个控件**。
- **同区域另一条被压的文本道**：模式标签 `SVAs4q_label`（"Creator mode"）。390px 实测：盒子 86px、内容 103px、`padding-left:18px` 是给图标的固定槽 → 字素级测量（Range.getBoundingClientRect 逐字）显示只有 `C r e a t o r _ m o d` 被绘制，**末字被祖先裁掉**（不是本层 ellipsis，本层 `text-overflow:ellipsis` 因裁剪发生在祖先而永不触发）。这就是"看着像被覆盖 / 出现无意义碎片"的机制。
- **修复**：给该道设可读下限 `min-width: min(104px, 30vw)`。修后实测 **104px，全部 11 个字素都在框内**（`all: "Creatormode"` / `visible: "Creatormode"`）；标题道相应从 86px 让到 68px 仍正常省略号。
- 说明：同一轮我曾给 jobs 计数道加 `min-width: max-content`，但**无法在本机复现该控件所在位置**（本会话当前无运行中后台任务；注入探针被 React 回收，计算宽度恒 0）→ 按"不做无法证实的修改"**撤回**该条。用户侧再现时再补。

### 10.2 标签行收起（已修，用户已确认要这么做）

- 0.1.5 把视图标签拆到独立网格轨（`grid-template-rows: 44px 44px`），两个原生标签时这一行白占 44px（header 97px = 8 + 44 + 8 + 44 + 1）。
- 规则：header 内 `[class*="wSkVaW_tabs"]` 的第 3 个 `button` 存在 ⟺ 展开。≤2 标签时 `display:none` + `grid-template-rows: 44px 0px !important`；≥3 时恢复 `44px 44px !important`。
- **必须显式覆盖 `min-height`**：宿主把 header 钉在 `min-height:76px`（该值恰好等于两行高度），只塌轨不解除下限时 header 停在 76px（第一次实测 97→76，未达预期）→ 折叠分支加 `min-height:0 !important` 后 97→**53px**。
- 用 `:nth-child(3)` 计数而非数据属性：不需要任何 effect 同步状态，React 增删标签后 CSS 自动重判。
- 门（`~/tmp/diag/verify-tabrow.mjs`，4 断言全过）：2 标签 → `tabsDisplay:none / rows=44px 0px / headerH=53`；注入第 3 个标签 → `flex / 44px 44px / headerH=97`；页面无滚动。
- 桌面门（`desktop-tabs.mjs`）：1280px 鼠标 → `framePresent:false`、`tabsDisplay:flex`、`headerH=76`、`gridRows:none` → **插件规则完全未作用于桌面**。

### 10.3 方法学教训：点击链探针的假阴性（未修完，已记录）

- 第一版 `clickchain.mjs` 报"31 个控件里 27 个点了没反应"——**结论错误**。
- 根因：探针在页面线程外分两次 `Runtime.evaluate`（先取控件表、再按 rect/aria 匹配点击），而 0.1.5 的 header 与消息区在两次调用之间会重渲染 → 匹配器返回 `lost`，被当成"点了没反应"。真正验证到的只有 4 个控件（toggle / files / Commands / Access mode）有状态变化。
- 正确的形状（本轮已改用）：**取控件 + 点击 + 采样必须在同一次 `Runtime.evaluate` 内完成**，控件索引在页内确定（`const IDX = n`），状态采样用「帧内计数 + 属性变更计数」而不是 `innerText.length`（后者含流式输出，噪声大）。第二版探针已按此改造；进一步加固（属性观察器计入弹层）在 `clickchain2.mjs`。
- 用户已确认"点不动的控件"这条诉求仍然有效，**具体是哪个控件待用户指明**（本轮用户给的是截图；截图里未标出被点的控件）。

### 10.4 本轮回归汇总

| 门 | 结果 |
|---|---|
| `pnpm verify` / `pnpm test:core` | ✅ / ✅ **87 pass / 0 fail** |
| `pnpm build` | ✅（rev `af3975dfe14f`） |
| `pnpm smoke:cdp` | ✅ 13 pass / 1 skip / **new=0** |
| 标签行门 `verify-tabrow.mjs` | ✅ 4/4 |
| 桌面门 `desktop-zero.mjs` | ✅ 7/7 |
| 桌面标签门 `desktop-tabs.mjs` | ✅ 未触碰 |
| 模式标签可读性 `verify-floors.mjs` | ✅ 104px / 11 字素全可见 |


---

## 11. 第四轮：撤回误改 + 顶栏两个"点不动"的真根因

### 11.1 撤回：标签行收起是错的（用户反馈「对话/轨迹栏消失了」）

§10.2 的"≤2 标签收起标签行"按用户先前选择实施，但用户随后明确反馈**轨迹栏消失**——那一行是切 Chat/Trajectory 的**唯一入口**，44px 是功能不是空白。**已整块撤回**（33 行全部删除，`wSkVaW_tabs` 在 layout.css.ts 归零），实测恢复：`tabsDisplay:flex`、header 97px、两个标签都可命中。
教训：**"省空间"类改动必须确认它隐藏的是不是用户的唯一入口**；`:nth-child` 计数门虽然让标签多时自动回来，但用户在 2 标签状态下就已经失去了入口。

### 11.2 文件按钮：宿主换控件导致的"点了没反应"（已修）

- 宿主在两种状态下**换按钮**（实测）：面板**关**时只有 `data-sidebar-right-expand`（0×0，但 click 有效）；面板**开**时 expand **被卸载**，只剩 `data-sidebar-right-toggle`（28×28，"收起右侧边栏"）。
- 我原本只点 expand → **面板已经开着时点我们的 Files 按钮 = 空操作**。而且面板是全屏的，会盖住 header，所以"第二次点不动"也由此而来。
- 修法：`openFilesPanel()` 改**开关语义**（有 opener 点 opener，否则点 closer），单测补两条（已开时用 closer / 两者都在时优先 opener）。真机门 `verify-files-toggle.mjs` 4/4：关→开→关→再开。

### 11.3 "右上三个点"：宿主把它自己的下拉菜单裁掉了（宿主 bug，已在我们这侧绕过）

- 该按钮是**官方 `@deepseek-ai/dsh-session-log-export`** 的 More actions（`nL4_yW_moreButton`，内含三段 2px SVG 组成 `···`），菜单项只有一项 `Download session log`。
- **真根因**：宿主把菜**单挂在 44×44 的 `wSkVaW_headerUtilities` 格子里，而该格子 `overflow: auto`** → 218×52 的菜单被裁进 44×44，**完全不绘制**。铁证：菜单计算样式 `flex / visible / opacity 1`，但 `elementsFromPoint(菜单项中心)` 的绘制栈里**没有菜单**，最上层是 `wSkVaW_tabs`；`elementsFromPoint` 全栈只有 tabs/header/centerCol/frame/body/html。
- **为什么提高 z-index 无效**（实测 7 种方案全败：`z-index:2000`、`isolation:isolate`、`position:fixed`、tabs z=0、utilities z=300、tabs pointer-events:none…）：菜单被困在 utilities 的 **z=2 堆叠上下文**里，而 tabs 是 `position:relative; z-index:1` 且 DOM 在后 → tabs 恒压菜单。
- **有效修法（一条属性）**：`[class*="wSkVaW_headerUtilities"] { overflow: visible }` → 菜单按宿主原本的定位绘制出来；真实触摸点击菜单项 → 菜单关闭并打开导出对话框（`menus 1→0, dialogs 1`）。
- **随用户指示隐藏该按钮**：用户指出"导出会话日志已在抽屉页脚做了"，故移动端 `[class*="nL4_yW_moreButton"] { display: none }`（桌面保持官方原样，实测 `moreVisible:true`）。`overflow: visible` 保留——它修的是宿主格子裁自家下拉的 bug，任何插件往同一格子挂下拉都会被裁。
- 验证：`verify-more-menu.mjs` 4/4（按钮在场 → 真实触摸开菜单 → 菜单项可达 → 真实触摸开对话框）；`verify-more-gone.mjs` 3/3（移动端隐藏、尾部无死区 `rowRight 374 / lastChildRight 372`、抽屉 Session log 仍在且可点）。

### 11.4 用户测量的对照结论

用户提供的像素级报告（画布 ≈1240×690）与 DOM 实测**逐项吻合**（1240 ÷ 3.18 ≈ 390 CSS px；其 #1 侧栏开关 48–95 → 15–30 CSS = 我们的 toggle [8,36]；#2 异常字形 308–338 → 97–106 = crumb 起点 80–158 区段）。其"B-1 左侧死区 213px → 67 CSS px"即宿主 header `padding-left:60px`（为已隐藏的 rail 开关预留）+ titleRow 起点，**属宿主设计而非渲染错误**；其 #9 溢出菜单 1108–1152 → 348–362 = `nL4_yW_moreButton` [330,374]，右侧 88px 是截图裁切余量。

### 11.5 第四轮回归汇总

| 门 | 结果 |
|---|---|
| `pnpm verify` / `pnpm test:core` | ✅ / ✅ **89 pass / 0 fail** |
| `pnpm build` | ✅（rev `bfd626b21389`） |
| `pnpm smoke:cdp` | ✅ 13 pass / 1 skip / **new=0** |
| 文件开关门 `verify-files-toggle.mjs` | ✅ 4/4 |
| 三点菜单门 `verify-more-menu.mjs` | ✅ 4/4 |
| 隐藏三点门 `verify-more-gone.mjs` | ✅ 3/3 |
| 桌面门 `desktop-tabs.mjs` | ✅ tabs 与 more 均未被触碰 |


---

## 12. 第五轮：「后台任务运行中 ⌄ 展开失效」取证（2026-09-13）

### 12.1 找到真凶：**是我们自己的两条规则**

| 规则 | 后果 | 证据 |
|---|---|---|
| chip 族 `position: static !important` | 废掉宿主锚点 | 宿主 `dsh-client-ui-jobs` 的面板 CSS 是 `position:absolute; top:calc(100% + 5px); left:0`，**依赖官方 `.root{position:relative}`**；A/B：`static` → 面板 `[8,849]`（视口高 844，**在屏幕外**）且不锚定；`relative` → `[271,57]` 正好在 chip 下方 |
| 同族菜单 `left: 8px !important` | 把面板推出右缘 | chip 在 header 右端（实测右缘 374/390），336px 面板 `left:0` 起于 chip 左缘 → 越界；`left:8px` 夹取实测 `x=350..686`（更糟），`right:8px` 实测 `x=30..366`（在视口内） |

两条都已修：删掉 `position: static`（原为 composer 的 ContextMeter 写，实测该处本就 `relative`、A/B 像素一致，删除无副作用）；菜单改 `left:auto; right:8px`。

### 12.2 修复后实测（真实 header 容器 + 宿主真实 CSS 契约）

```
host 格子 [245,8,129,44]   root position=relative   menu [30,57,336,73]
anchored=true（chip 下缘 52 + 5）   insideViewport=true
```

### 12.3 仍存疑的一层（未在真机确认，不假装已修）

面板顶部（y 57–96）与官方 view-tabs 行（y 52–96）重叠，`elementFromPoint` 命中的是 `wSkVaW_tabs`。**注意：subagent chip 不受影响**——它的菜单 portal 到 `BODY` + `position:fixed`，实测真实触摸开合正常（`[38,49,336,58]` 完全在视口内、绘制栈含自身）。该重叠只在 jobs 的 header 内 `absolute` 形态上观察到，且用合成 fixture 复现不稳定（fixture 的绝对定位与 React 真实渲染不一致），故**不以 fixture 结论当事实**。

### 12.4 方法论教训（本轮最大成本来源）

- **不要用合成 DOM 替代真实渲染去验证定位/叠层**：我先后 4 次用注入 fixture 得出结论，其中至少一次（`top:-40`）是 fixture 位置计算失真导致的误判。
- **确认目标控件存在于被测环境**，再动手：本机 profile 原本没有 live 任务，我在"控件不存在"的环境里排查了很久。建立回路的正确做法是先让目标控件真实出现（起一个真后台任务 + 用有该任务的会话 ID）。
- 反引号（Pitfalls §CSS 模板）本轮连踩三次，已加 `tests/docs-consistency.test.ts` 守卫（每个 CSS 模板必须恰好两个反引号）。
